# API Rules

REST + OpenAPI. Versioned endpoints. Idempotency for retryable commands. Authorization server-side. Correlation IDs. Standard errors. Cursor pagination for high-volume lists. External interoperability contracts isolated from internal domain APIs.
